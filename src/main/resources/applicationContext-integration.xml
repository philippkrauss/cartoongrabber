<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:applicationContext.properties" />

    <int:gateway id="grabberService"
                 service-interface="cartoongrabber.service.GrabberService"
                 default-request-channel="inputChannel"/>

    <int:channel id="inputChannel"/>

    <int:transformer input-channel="inputChannel" output-channel="definitionChannel">
        <beans:bean class="cartoongrabber.transformer.SourceToDefinitionTransformer"/>
    </int:transformer>

    <int:channel id="definitionChannel"/>

    <int:transformer input-channel="definitionChannel" output-channel="cartoonChannel">
        <beans:bean class="cartoongrabber.transformer.DefinitionToCartoonTransformer"/>
    </int:transformer>

    <int:channel id="cartoonChannel"/>

    <int:service-activator input-channel="cartoonChannel">
        <bean class="cartoongrabber.service.FileCollection">
            <constructor-arg value="${conf.filecollection.baseDir}" />
        </bean>
    </int:service-activator>

</beans>